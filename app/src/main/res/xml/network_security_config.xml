<?xml version="1.0" encoding="utf-8"?>
<!--
  HTTPS sigue siendo obligatorio para todo el tráfico a internet. La única excepción es
  el tráfico plano a un servidor de IA que corre en el propio teléfono o en la red local
  (Nodo, LM Studio, Ollama): esos endpoints no tienen certificado y nunca salen del
  dispositivo o de la LAN del usuario.
-->
<network-security-config>
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>

    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
        <domain includeSubdomains="false">10.0.2.2</domain>
    </domain-config>
</network-security-config>
